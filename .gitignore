__pycache__/
*.py[cod]

# Runtime-only files. Downloaded repositories, snapshots, and catalog state are
# intentionally tracked as a publishable snapshot.
logs/
state/sync-loop.pid
repositories/**/.sync-git/
